{
  "name": "merge-ical",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "sst": "^3",
    "typescript": "^5.8.0",
    "aws-sdk": "^2"
  }
}
